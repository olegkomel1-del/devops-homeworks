---
- name: Test custom module via playbook
  hosts: localhost
  connection: local
  tasks:
    - name: Create file using my_own_module
      my_own_module:
        path: "/tmp/playbook_test.txt"
        content: "Hello from Ansible Playbook!"
